// Supabase client scaffold — no project is wired up yet, so nothing in the
// app calls this client. Once a real project exists:
//   1. Add VITE_SUPABASE_URL / VITE_SUPABASE_ANON_KEY to a .env file
//   2. Uncomment the createClient call below
//   3. Replace the mock state in CommunityPage/ShopPage with real
//      `supabase.from(...)` queries (each call site has a TODO marking
//      exactly which query belongs there)
//
// import { createClient } from '@supabase/supabase-js'
//
// export const supabase = createClient(
//   import.meta.env.VITE_SUPABASE_URL,
//   import.meta.env.VITE_SUPABASE_ANON_KEY,
// )

export {}
